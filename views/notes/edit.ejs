<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Note</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-indigo-900 to-blue-500 min-h-screen flex flex-col items-center p-10">

  <a href="/notes" class="mb-6 bg-indigo-600 text-white px-6 py-3 rounded-lg font-bold">Back to Notes</a>

  <h1 class="text-3xl font-bold text-white mb-6">Edit Note</h1>

  <form class="w-full max-w-lg" method="POST" action="/notes/edit/<%= note._id %>">
    <input type="text" name="title" value="<%= note.title %>" required class="mb-3 px-4 py-2 w-full rounded">
    <textarea name="content" class="mb-3 px-4 py-2 w-full rounded"><%= note.content %></textarea>
    <input type="text" name="tags" value="<%= note.tags.join(', ') %>" class="mb-3 px-4 py-2 w-full rounded">
    <button type="submit" class="bg-green-600 text-white px-6 py-2 rounded w-full">Update Note</button>
  </form>

</body>
</html>
